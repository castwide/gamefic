#!/usr/bin/env ruby

require "core/game.rb"

if ARGV[0] == nil
	puts "Game file required."
	exit
end

module Gamefic
	story = Story.new
	story.load(ARGV[0])
	player = Player.new
	player.name = "joejoe"
	story.introduce player
	game = Game.new
	game.run(player)
end
